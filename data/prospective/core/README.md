# Prospective 001 local evidence

This directory is the local, read-only evidence store defined by
`MATVIX_PROSPECTIVE_001_CONTRACT.md`.

Runtime `PREDICTION_PUBLISHED` and `OUTCOME_RESOLVED` JSON files are intentionally
ignored by Git because they are machine-local evidence and may not be backfilled.
Only this provenance note is tracked.
